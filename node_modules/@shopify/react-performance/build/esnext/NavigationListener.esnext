import { useNavigationListener } from './navigation-listener.esnext';

function NavigationListener({
  onNavigation
}) {
  useNavigationListener(onNavigation);
  return null;
}

export { NavigationListener };
