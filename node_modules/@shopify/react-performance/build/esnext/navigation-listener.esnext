import { usePerformanceEffect } from './performance-effect.esnext';

function useNavigationListener(listener) {
  usePerformanceEffect(performance => performance.on('navigation', listener));
}

export { useNavigationListener };
