export declare class FakerFactory<T> {
    private factory;
    private _fixture;
    constructor(factory: () => T);
    new(override?: Partial<T>): T;
    array(total: number, override?: Partial<T> | ((index: number) => Partial<T>)): T[];
    fixture(id: string | number, override?: Partial<T>): T;
}
