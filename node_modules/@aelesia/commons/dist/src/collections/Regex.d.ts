export declare class RegexTest {
    static isoDate(string: string): boolean;
    static email(string: string): boolean;
}
export declare const Regex: {
    is: typeof RegexTest;
    ISO_DATE_RUBY: RegExp;
    ISO_DATE: RegExp;
    DATE: RegExp;
    EMAIL: RegExp;
};
