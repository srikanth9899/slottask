export declare class DateUtil {
    static now(): Date;
    static parse(date?: unknown): Date;
    private static fromTimestamp;
    static utc(date?: Date): number;
    static epoch(date?: Date): number;
    static add(ms: number, date?: Date): Date;
    static minus(ms: number, date?: Date): Date;
    static isBefore(date: Date, date2: Date): boolean;
    static isAfter(date: Date, date2: Date): boolean;
    static isPast(date: Date): boolean;
    static isFuture(date: Date): boolean;
    static _f(date: Date, format: string): string;
}
