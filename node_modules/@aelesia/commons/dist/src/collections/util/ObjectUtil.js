"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ObjectUtil = void 0;
var ObjectUtil = (function () {
    function ObjectUtil() {
    }
    ObjectUtil.omit = function (object, omitKeys) {
        var newObject = Object.assign({}, object);
        if (omitKeys instanceof Array) {
            omitKeys.forEach(function (key) {
                delete newObject[key];
            });
        }
        else {
            delete newObject[omitKeys];
        }
        return newObject;
    };
    ObjectUtil.pick = function (object, pickKeys) {
        var newObject = {};
        if (pickKeys instanceof Array) {
            pickKeys.forEach(function (key) {
                newObject[key] = object[key];
            });
        }
        else {
            newObject[pickKeys] = object[pickKeys];
        }
        return newObject;
    };
    return ObjectUtil;
}());
exports.ObjectUtil = ObjectUtil;
//# sourceMappingURL=ObjectUtil.js.map